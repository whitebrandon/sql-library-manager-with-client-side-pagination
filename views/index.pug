extends layout

block content
        include included/search-form
        h1 Books
        p
            a.button.new-book(href='/books/new') Add New Title
        table.content-table-index
            thead
                tr
                    th Title
                        span.instructions  (Click Title to Update Book Details)
                    th Author
                    th Genre
                    th Year
            tbody
                each book, i in books
                    tr
                        td
                            - var offset = (active * 10)
                            a(href=`/books/${book.id}`)= i+1 + ': ' + book.title
                        td= book.author
                        td= book.genre
                        td= book.year